export const LABELS = {
    idle: "Play",
    running: "Pause",
    paused: "Play",
    restart: "ArrowCounterClockwise",
    skip: "SkipForward"
} as const;
export const SKIP_MODE_BUTTON_DURATION_MILLISECONDS = 250;
